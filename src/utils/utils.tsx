export const getData = async (key: string) => {
  try {
    const value = "";
    if (value !== null || value !== undefined) {
      return value;
    }
  } catch (e) {
    // error reading value
  }
};
